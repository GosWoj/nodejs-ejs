<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/header.ejs") %>
  </head>
  <body>
    <%- include("includes/nav.ejs") %>
    <main>
      <% if (names.length > 0) { %> <% for (let name of names) { %>
      <h1><%= name.name %></h1>
      <% } %> <% } else { %>
      <h1>No names available</h1>
      <% } %>
    </main>
  </body>
</html>
